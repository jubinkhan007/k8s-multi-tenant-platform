apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: team-a-app
  namespace: team-a
spec:
  hosts:
  - "team-a.company.com"
  gateways:
  - istio-system/shared-gateway
  http:
  - route:
    - destination:
        host: team-a-service
        port:
          number: 80
