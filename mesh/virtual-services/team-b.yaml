apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: team-b-app
  namespace: team-b
spec:
  hosts:
  - "team-b.company.com"
  gateways:
  - istio-system/shared-gateway
  http:
  - route:
    - destination:
        host: team-b-service
        port:
          number: 80
